name: bingo
description: a very exciting bing game

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  cupertino_icons: ^1.0.2
  go_router: ^5.2.4
  firebase_core: ^2.4.0
  cloud_firestore: ^4.2.0
  collection: ^1.16.0
  flutter_riverpod: ^2.1.1
  google_fonts: ^3.0.1
  flutter_sequence_animation: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
